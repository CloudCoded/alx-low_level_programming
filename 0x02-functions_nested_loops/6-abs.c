#include "main.h"
/**
 * _abs - computes the abs value of an int
 * @n: the int in question
 *
 * Return: the abs value of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
